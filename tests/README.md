# Tests

Hardware-independent native tests will live here. Early candidates include timebase wrap logic, task registration, scheduling, state transitions, faults, logging queues, protocol parsing, DShot encoding, receiver parsing, mixing, and control algorithms as their milestones are approved.

Hardware tests remain separate and must not be represented as passing host tests.
