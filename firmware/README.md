# Firmware

This directory will contain the operational embedded firmware and its `app`, `flight`, `peripherals`, and `hardware` source boundaries.

Milestone 0.1 intentionally contains no STM32 target or application source. The cross-build and smallest boot path belong to Milestone 0.2.
